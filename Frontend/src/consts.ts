const ADDRESS = {
  NODE: process.env.REACT_APP_NODE_ADDRESS as string,
};

const AI_API_KEY = {
  KEY: process.env.AI_API_KEY as string,
};

const LOCAL_STORAGE = {
  ACCOUNT: "account",
};

export { ADDRESS, AI_API_KEY, LOCAL_STORAGE };
